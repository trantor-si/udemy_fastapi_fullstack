clear
pip install -U pip # This will update pip to the latest version
pip install pipenv
export PIP_VENV_IN_PROJECT="enabled" # This will create a virtual environment in the project directory
mkdir .venv
pipenv shell # This will activate the virtual environment
